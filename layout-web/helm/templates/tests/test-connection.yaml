apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "layout-web.fullname" . }}-test-connection"
  labels:
    {{- include "layout-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "layout-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
