apiVersion: v1
kind: Service
metadata:
  name: {{ include "layout-web.fullname" . }}
  labels:
    {{- include "layout-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "layout-web.selectorLabels" . | nindent 4 }}
