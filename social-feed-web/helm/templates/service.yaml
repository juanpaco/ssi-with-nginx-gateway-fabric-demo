apiVersion: v1
kind: Service
metadata:
  name: {{ include "social-feed-web.fullname" . }}
  labels:
    {{- include "social-feed-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "social-feed-web.selectorLabels" . | nindent 4 }}
