apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "social-feed-web.fullname" . }}-test-connection"
  labels:
    {{- include "social-feed-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "social-feed-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
