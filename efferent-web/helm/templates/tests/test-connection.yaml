apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "efferent-web.fullname" . }}-test-connection"
  labels:
    {{- include "efferent-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "efferent-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
