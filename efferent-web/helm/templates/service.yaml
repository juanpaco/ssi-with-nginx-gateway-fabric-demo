apiVersion: v1
kind: Service
metadata:
  name: {{ include "efferent-web.fullname" . }}
  labels:
    {{- include "efferent-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "efferent-web.selectorLabels" . | nindent 4 }}
